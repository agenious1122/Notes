//Union Types
//if we want to assign multiple datatypes to a variable we use union datatype
//in union data type we can assign number, string and bollean data type to one varialble at once
let userName : number | String | Boolean = "Adnan"
console.log(userName);

export{}